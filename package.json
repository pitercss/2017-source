{
  "private": true,
  "scripts": {
    "start": "gulp",
    "build": "gulp build",
    "deploy": "gulp deploy",
    "test": "editorconfig-cli && gulp build && ls dest"
  },
  "devDependencies": {
    "@htmlacademy/editorconfig-cli": "0.0.8",
    "autoprefixer": "^6.5.3",
    "browser-sync": "^2.17.5",
    "gulp": "^3.9.1",
    "gulp-pug": "^3.2.0",
    "bemto.pug": "^2.0.0",
    "gulp-posthtml": "^1.5.2",
    "gulp-replace": "^0.5.4",
    "gulp-rsync": "0.0.6",
    "gulp-stylus": "^2.6.0",
    "posthtml-minifier": "^0.1.0",
    "stylobuild": "^1.0.0"
  },
  "editorconfig-cli": [
    "src/*.html",
    "src/**/*.css",
    "src/**/*.svg"
  ]
}
